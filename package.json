{
  "name": "portfolio-fullstack",
  "version": "1.0.0",
  "description": "leeland's portfolio",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "concur": "concurrently \"nodemon server.js\" \"cd client && npm start\"",
    "deploy": "npm run build && git add . && git commit -m \"Production Build\" && git push heroku master",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "mongodb",
    "express",
    "react",
    "node"
  ],
  "author": "leelandmiller",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^1.0.3",
    "body-parser": "^1.18.2",
    "express": "^4.16.1",
    "express-session": "^1.15.6",
    "mongoose": "^4.12.0",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0",
    "react-redux": "^5.0.6",
    "react-router-dom": "^4.2.2"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
